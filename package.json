{
  "name": "fisheye",
  "version": "1.0.0",
  "description": "Site web de photographes freelances",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch ./sass/main.scss:src/css/style.css --style compressed",
    "prefix": "postcss ./src/css/style.css --use autoprefixer -d src/css/prefixed/"
  },
  "author": "Loic Antoniak",
  "license": "ISC",
  "browserslist": "last 4 versions",
  "dependencies": {
    "express": "^4.17.1",
    "sass": "^1.32.8"
  }
}
